<view class="container">
  <button type="primary" bindtap="generate">生成长图</button>
  <view wx:if="{{url}}" class="preview">
    <image mode="widthFix" src="{{url}}" />
    <button style="margin-top:12px" bindtap="save">保存到相册</button>
  </view>
  <ad-banner slot="result" daily="3" />
</view>
